import { SportBaseDetailStore } from './sport-base-detail-store';

describe('SportBaseDetailStore', () => {
  it('should create an instance', () => {
    expect(new SportBaseDetailStore()).toBeTruthy();
  });
});
